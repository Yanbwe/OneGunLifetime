# 更新日志

## [1.0.0] - 2026-08-27

- 新增灵魂绑定玩法：玩家可用命令绑定一把枪，此后背包只保留这一把投影枪。
- 新增属性常驻玩家：数值与插件加成不再依赖手持，空手也生效。
- 新增其它枪械同化：进入背包的 ModularShoot 枪会自动转为绑定枪并保留插件。
- 新增丢失补回：丢弃立即补回，岩浆/销毁后自动补回。
- 新增容器守卫：投影枪无法放入容器，漏放会被低频扫描回收。
- 新增非持有者禁射：他人拾取后无法发射，未绑定者会得到纪念品。
- 插件安装/拆卸与灵魂数据双向同步，创造菜单等路径由扫描兜底。
- 新增同化射击谓词：绑定玩家扣扳机打外部枪会被拒绝并立即同化。
- 新增 Java 开发者 API（`OneGunLifetimeAPI`）：其他模组可直接绑定/解绑、调整属性特性、装卸插件、读取数值面板，不再局限于指令。
- 命令实现全面下沉至 API，行为不变。
- 开发者 API 优化：卸载插件改为把插件物品返还给玩家，并返回结构化拒绝原因；枪械状态写入不再抛异常，改用结果对象；数值面板查询保持属性注册表顺序。
- 修正 `/onegun stat set` 数值非法时误报"属性未注册"的提示。